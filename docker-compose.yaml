version: '3.0'

services:
  grafana:
    container_name: 'extended-flow-panel'
    platform: 'linux/amd64'
    build:
      context: ./.config
      args:
        grafana_image: ${GRAFANA_IMAGE:-grafana-enterprise}
        grafana_version: ${GRAFANA_VERSION:-10.0.0}
    ports:
      - 3001:3000/tcp
    volumes:
      - ./dist:/var/lib/grafana/plugins/extended-flow-panel
      - ./provisioning:/etc/grafana/provisioning
